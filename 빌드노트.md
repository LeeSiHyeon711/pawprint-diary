# 빌드노트 — PawprintDiary

## 프로젝트 정보
- 스택: Next.js 14 App Router + React 18 + TypeScript + Tailwind CSS
- 위치: `projects/PawprintDiary/05-개발/` (이 폴더가 Next.js 프로젝트 루트)

---

## 실행 방법

### 1. 의존성 설치
```bash
cd projects/PawprintDiary/05-개발
npm install
```

### 2. 개발 서버 실행
```bash
npm run dev
```
브라우저에서 `http://localhost:3000` 접속

### 3. 프로덕션 빌드 검증
```bash
npm run build
```
타입/빌드 에러 0으로 통과해야 FEAT 완료 조건 충족 (설계서 7장 전역 규칙)

---

## 환경 변수 설정

`.env.example`을 복사해 `.env.local`로 만들고 API 키를 입력합니다:
```bash
cp .env.example .env.local
# .env.local 에서 ANTHROPIC_API_KEY= 에 실제 키 입력
```

**ANTHROPIC_API_KEY가 없어도 Mock 모드로 전체 UI 흐름이 동작합니다** (실제 Mock 구현은 FEAT-05).  
키를 넣으면 실제 `claude-opus-4-8` 모델을 호출합니다.

---

## 이슈별 작업 기록

### FEAT-01 — 프로젝트 셋업 + 디자인 시스템 (이슈 #1)
**추가 문서 읽기 이유 기록**: FEAT 문서가 "설계서 4-3 컬러표 그대로"를 명시했으나 hex 값이 FEAT 문서에 없어 설계서를 1회 추가 확독.

**구현 완료 파일 목록**:
- `package.json` — 의존성 정의 (next@14, react@18, tailwindcss, idb, @anthropic-ai/sdk, date-fns)
- `next.config.mjs` — Next.js 기본 설정
- `tsconfig.json` — TypeScript + path alias(@/*)
- `tailwind.config.ts` — 디자인 토큰 매핑 (colors, borderRadius, boxShadow, fontFamily)
- `postcss.config.mjs` — PostCSS (Tailwind + Autoprefixer)
- `.env.example` — ANTHROPIC_API_KEY(빈 값), ANTHROPIC_MODEL=claude-opus-4-8
- `app/globals.css` — CSS 변수 토큰 + Tailwind 베이스 + Pretendard CDN
- `app/layout.tsx` — 루트 레이아웃 (PetProvider 임시 + BottomNav)
- `app/page.tsx` — 임시 환영 페이지 (/today 링크 포함)
- `components/ui/Button.tsx` — variant(primary/soft/ghost), full prop
- `components/ui/Card.tsx` — radius 16, surface 배경, 옅은 그림자
- `components/ui/Tag.tsx` — pill 칩 (선택형/표시형)
- `components/ui/Field.tsx` — label + children 묶음
- `components/ui/EmptyState.tsx` — 빈 상태 안내 (🐾 기본 아이콘)
- `components/ui/Spinner.tsx` — 로딩 스피너
- `components/ui/SectionTitle.tsx` — 섹션 제목
- `components/BottomNav.tsx` — 하단 4탭 (오늘/기록/질문/프로필, 활성 표시)
- `components/AppHeader.tsx` — 상단 헤더 (타이틀 + 옵션 children)
- `lib/petContext.tsx` — PetProvider/usePet 임시 골격 (FEAT-03에서 교체 예정)

**빌드 결과**: Bash 차단으로 직접 실행 불가. 공방장이 아래 명령으로 검증 필요:
```bash
cd projects/PawprintDiary/05-개발 && npm install && npm run build
```

**다음 이슈**: FEAT-02 (로컬 저장 계층 — IndexedDB)

---

### FEAT-02 — 로컬 저장 계층 IndexedDB (이슈 #2)

**추가 문서 읽기 이유 기록**: FEAT-01에서 만든 `lib/petContext.tsx` 기존 구조 확인 목적으로 lib/ 폴더 파일 목록을 1회 확인. 타입 정의 중복 방지를 위한 최소 탐색.

**구현 완료 파일 목록**:
- `lib/types.ts` — 도메인 타입 신규 생성
  - 유니온: Species / Gender / Appetite / Activity / Sleep / Toilet
  - 인터페이스: Pet / AISummary / DiaryEntry / AIConversation / MetaRecord
  - Blob 필드(profile_image, photos)에 AI 전송 금지 주석 명시 (보정 지시 4)
  - date: YYYY-MM-DD, created_at: ISO 문자열 타입 주석 (보정 지시 7)
- `lib/db.ts` — IndexedDB 초기화 신규 생성
  - PawprintDiaryDB DBSchema 타입 (idb v8)
  - 4개 스토어: pets(keyPath: pet_id) / entries(keyPath: entry_id) / conversations(keyPath: conversation_id) / meta(keyPath: key)
  - 인덱스: entries.by-pet(pet_id) / entries.by-pet-date([pet_id, date]) / conversations.by-pet(pet_id)
  - 싱글톤 Promise 캐시 + 실패 시 캐시 초기화 (재시도 가능)
- `lib/repos.ts` — Repository 함수 신규 생성
  - metaRepo: get / set
  - petRepo: create / update / get / getActive (metaRepo.activePetId 연계)
  - entryRepo: save / update / get / remove / listByPet / recentByPet
  - conversationRepo: add / listByPet
  - generateId(): crypto.randomUUID + 타임스탬프 폴백
  - sortEntriesDesc(): date desc → created_at desc (보정 지시 7)

**빌드 우려 지점**:
1. idb v8 복합 인덱스 타입 `'by-pet-date': [string, string]` — `createIndex('by-pet-date', ['pet_id', 'date'])` 호출 시 idb가 `keyPath: string | Iterable<string>`을 수용하므로 런타임 문제 없음. 단, idb v8 내부 TypeScript 타입 추론이 복합 인덱스 keyPath에 대해 까다로울 수 있음 → `npm run build` 결과 확인 필요.
2. `db.createObjectStore('entries', { keyPath: 'entry_id' })` 반환값 타입 — idb v8 upgrade 콜백 내 타입 추론. 문제 발생 시 `as IDBObjectStore` 캐스팅 또는 별도 트랜잭션 활용 필요.

**빌드 결과**: Bash 차단으로 직접 실행 불가. 공방장이 아래 명령으로 검증 필요:
```bash
cd projects/PawprintDiary/05-개발 && npm run build
```

**다음 이슈**: FEAT-03 (반려동물 프로필 화면)

---

### FEAT-03 — 반려동물 프로필 등록/수정 (이슈 #3)

**추가 문서 읽기 이유 기록**: 기존 FEAT-01/02 코드(petContext.tsx, types.ts, repos.ts, UI 컴포넌트)를 이어쓰기 위해 확인. tailwind.config.ts는 커스텀 클래스명(rounded-input, rounded-pill 등) 확인 목적으로 추가 확독.

**구현 완료 파일 목록**:
- `lib/petContext.tsx` — FEAT-01 임시 골격을 실구현으로 교체
  - `PetContextType = { pet?: Pet; loading: boolean; refresh: () => Promise<void> }` 확정
  - `PetProvider`: 마운트 시 `petRepo.getActive()` 로딩, refresh로 재로딩
  - `usePet()`: 확정 시그니처 반환
- `lib/imageUtils.ts` — 신규 생성
  - `resizeImageToBlob(file, maxSize, quality)`: canvas 리사이즈 후 JPEG Blob 반환
  - 긴 변이 maxSize 초과 시 비율 유지 축소, 미초과 시 JPEG 변환만 수행
- `components/BlobImage.tsx` — 신규 생성
  - `BlobImage({ blob?, alt, className? })`: Blob → objectURL → img 렌더링
  - useEffect로 URL.createObjectURL, cleanup에서 revokeObjectURL
  - blob 없으면 🐾 플레이스홀더
- `components/PhotoUpload.tsx` — 신규 생성
  - 단일/다중 discriminated union 타입 (`mode: 'single' | 'multi'`)
  - 파일 선택 시 resizeImageToBlob(800, 0.85) 호출
  - 리사이즈 실패 시 인라인 에러 메시지 표시 (FEAT-03 예외처리)
  - multi 모드 골격 구현 (FEAT-04에서 실사용)
- `components/ProfileForm.tsx` — 신규 생성
  - `ProfileFormValues = Omit<Pet, 'pet_id' | 'created_at'>` 타입 export
  - 필수: name, species, age, gender → 미충족 시 저장 버튼 disabled + 각 필드 안내 문구
  - 종(강아지/고양이/기타), 성별(수컷/암컷/중성화 수컷/중성화 암컷) Tag 선택
  - 선택: breed, personality, likes, dislikes, health_notes, profile_image
  - PhotoUpload(single mode) 통합
- `app/profile/new/page.tsx` — 신규 생성
  - `petRepo.create` → `metaRepo.set('activePetId')` → `refresh()` → `/today` 이동
- `app/profile/edit/page.tsx` — 신규 생성
  - 마운트 시 `petRepo.getActive()` 로딩, 없으면 `/profile/new` 리다이렉트
  - `petRepo.update({ ...values, pet_id, created_at })` → `refresh()`

**확정 usePet 시그니처**:
```ts
usePet(): { pet?: Pet; loading: boolean; refresh: () => Promise<void> }
```

**빌드 결과**: `npm run build` 타입/빌드 에러 0 통과
```
Route (app)                              Size     First Load JS
┌ ○ /                                    175 B            96 kB
├ ○ /_not-found                          875 B            88 kB
├ ○ /profile/edit                        996 B          93.6 kB
└ ○ /profile/new                         764 B          93.4 kB
```

**빌드 우려 지점**: 없음. 모든 타입 에러 0.

**다음 이슈**: FEAT-04 (일기 작성 화면)

---

### FEAT-04 — 오늘의 발자국 작성 (이슈 #4)

**추가 문서 읽기 이유 기록**: 기존 컴포넌트(PhotoUpload, Tag, EmptyState, Button, Field, SectionTitle, Spinner, AppHeader, Card) 인터페이스·스타일 확인. tailwind.config.ts로 커스텀 클래스명 확인. petContext.tsx로 usePet 확정 시그니처 재확인. 모두 이어쓰기 목적의 최소 확독.

**구현 완료 파일 목록**:
- `components/StepSelector.tsx` — 신규 생성
  - 옵션 상수 export: `APPETITE` / `ACTIVITY` / `SLEEP` / `TOILET` (FEAT-02 유니온 일치)
  - `StepSelector<T extends string>` 제네릭 컴포넌트: 가로 칩 선택, 같은 값 재클릭 시 `undefined` 반환(해제)
- `components/MoodTagPicker.tsx` — 신규 생성
  - `MOOD_TAGS` 상수 6개 export
  - `MoodTagPicker({ value, onChange })`: Tag 토글 방식 복수 선택
- `components/DiaryForm.tsx` — 신규 생성
  - `DiaryFormValues = Omit<DiaryEntry, 'entry_id' | 'pet_id' | 'created_at' | 'ai_summary'>` export (보정 지시 5 — ai_summary 절대 미포함)
  - `DiaryForm({ initial?, onSubmit, submitting? })`: 날짜(필수·기본 오늘)·식욕·활동량·수면·배변(StepSelector)·기분(MoodTagPicker)·특이행동·자유일기·사진(PhotoUpload multi max=4)
  - 미래 날짜 경고 + 저장 버튼 disabled
- `app/today/page.tsx` — 신규 생성
  - `usePet()` → loading/no pet/폼 분기
  - activePet 없으면 EmptyState + `/profile/new` 유도
  - `entryRepo.save({ pet_id, ...values })` — ai_summary 미포함 저장 (보정 지시 5)
  - 저장 완료 후: "AI가 오늘의 발자국을 읽고 있어요 ✨" 플레이스홀더 (실제 AI 호출은 FEAT-06)
  - 저장 실패 시 한국어 토스트 + 폼 값 유지

**저장 흐름 (ai_summary 미포함 확인)**:
```
handleSubmit → entryRepo.save({ pet_id, date, appetite?, ..., mood_tags, photos })
                                                            ↑ ai_summary 없음
→ entries 스토어에 DiaryEntry 저장 (ai_summary 필드 없음)
→ savedEntryId 보관 → FEAT-06이 이 entry_id로 AI 호출 후 entryRepo.update로 ai_summary 병합 예정
```

**빌드 결과**: `npm run build` 타입/빌드 에러 0 통과
```
Route (app)                              Size     First Load JS
┌ ○ /                                    175 B            96 kB
├ ○ /_not-found                          875 B            88 kB
├ ○ /profile/edit                        1.02 kB        94.1 kB
├ ○ /profile/new                         760 B          93.9 kB
└ ○ /today                               10 kB           109 kB
+ First Load JS shared by all            87.1 kB
```

**빌드 우려 지점**: 없음. 제네릭 StepSelector(`<T extends string>`)의 TSX 파싱 문제 없음. 타입 에러 0.

**다음 이슈**: FEAT-05 (AI 요약 API)

---

### FEAT-05 — AI 프록시 + 가드레일 (이슈 #5)

**추가 문서 읽기 이유 기록**: `lib/types.ts`(PetLike/EntryLike의 기반 유니온 타입 확인), `.env.example`(ANTHROPIC_MODEL 기본값 확인) — 2개 추가 확독. 모두 FEAT 지시("lib/types.ts 참고 가능")에 명시된 범위.

**구현 완료 파일 목록**:
- `lib/guardrails.ts` — 신규 생성
  - `PetLike` / `EntryLike` 인터페이스 export (Blob 필드 제외, 보정 지시 4)
  - `sanitize(text)`: BANNED_REPLACE 5개 치환 + STRIP_SENTENCE 6개 패턴 문장 단위 제거
  - `needsVetNote(entry)`: Set 멤버십으로 appetite/activity/toilet 조건 판단 (AC-10)
  - `buildVetNote()`: 고정 권장 문구 반환
- `lib/prompt.ts` — 신규 생성
  - `SYSTEM_PROMPT`: 6개 규칙 (단정/말대리/의료진단/죄책감/종범위 등)
  - `buildSummaryPrompt(pet, entry)`: JSON only 요청 + 종 분기 지시 (강아지/고양이 특화, 기타 일반 관찰)
  - `buildAskPrompt(pet, question, recent)`: 프로필 + 최근 5개 기록 맥락 포함 (결정 #5)
- `app/api/ai/summary/route.ts` — 신규 생성
  - POST /api/ai/summary: 입력 유효성(400) → Mock/실모델 분기 → JSON 파싱(1회 재시도 → 폴백) → sanitize → vet_note 강제(AC-10) → 응답
  - 키 없음/호출 실패 모두 buildMockSummary()로 graceful 폴백
- `app/api/ai/ask/route.ts` — 신규 생성
  - POST /api/ai/ask: 입력 유효성(400) → Mock/실모델 분기 → sanitize → 응답
  - 키 없음/호출 실패 모두 buildMockAsk()로 graceful 폴백

**sanitize() 로직 요약**:
1. BANNED_REPLACE 5개 치환: 확실히→아마, 분명히→아마, 틀림없이→어쩌면, 100%→많이, 반드시→되도록
2. STRIP_SENTENCE 6개 패턴(진단/처방/말하고있어요/행복하대/용서했/용서해) → 해당 문장 전체 제거 (줄→문장 단위 분리 후 필터)
3. 모든 응답 경로(실모델·Mock·폴백) 마지막 단계로 공통 적용 (보정 지시 3)

**needsVetNote() 로직 요약**:
- appetite ∈ {적게 먹음, 거의 안 먹음} → true
- activity ∈ {조용함, 거의 움직이지 않음} → true
- toilet ∈ {묽음, 굳음, 이상 있음} → true
- 위 중 하나라도 해당 시 vet_note 강제 주입 (AC-10)

**Mock 동작 방식**:
- ANTHROPIC_API_KEY 미설정 또는 Anthropic SDK 호출 실패 시 동작
- summary: 입력의 appetite/activity/mood_tags를 인용한 완충형 4문장 + vet_note 조건부 포함
- ask: 최근 기록 count/날짜/activity/appetite + 프로필 personality 인용 완충형 2문단
- 두 Mock 모두 sanitize() 통과 (가드레일 형식 그대로 — 키 없이도 AC-06·07·08·09·10·11 검증 가능)

**빌드 결과**: `npm run build` 타입/빌드 에러 0 통과
```
Route (app)                              Size     First Load JS
┌ ○ /                                    176 B            96 kB
├ ○ /_not-found                          875 B            88 kB
├ ƒ /api/ai/ask                          0 B                0 B
├ ƒ /api/ai/summary                      0 B                0 B
├ ○ /profile/edit                        1.02 kB        94.1 kB
├ ○ /profile/new                         760 B          93.9 kB
└ ○ /today                               10 kB           109 kB
```

**빌드 우려 지점**: 없음. Anthropic.TextBlock 타입 predicate 사용으로 SDK 타입 추론 문제 없음. lookbehind 정규식 `(?<=[.!?])` Node 18+ 환경에서 정상 동작.

**키 없이 Mock 검증 curl 명령**:
```bash
# 서버 실행: cd projects/PawprintDiary/05-개발 && npm run dev
# AC-10 검증: 식욕↓+활동↓ → vet_note 존재
curl -s -X POST http://localhost:3000/api/ai/summary \
  -H 'content-type: application/json' \
  -d '{"pet":{"name":"콩","species":"강아지","age":"3살","gender":"수컷"},"entry":{"date":"2026-06-22","appetite":"거의 안 먹음","activity":"조용함","mood_tags":["피곤해 보임"]}}' | jq .

# species:"기타" — 일반 관찰 중심 확인
curl -s -X POST http://localhost:3000/api/ai/summary \
  -H 'content-type: application/json' \
  -d '{"pet":{"name":"별이","species":"기타","age":"2살","gender":"암컷"},"entry":{"date":"2026-06-22","appetite":"잘 먹음","activity":"매우 활발함","mood_tags":["신나 보임"]}}' | jq .

# AC-11: ask — 프로필+최근기록 맥락 반영
curl -s -X POST http://localhost:3000/api/ai/ask \
  -H 'content-type: application/json' \
  -d '{"pet":{"name":"콩","species":"강아지","age":"3살","personality":"활발함"},"question":"콩이 요즘 밥을 잘 안 먹는데 괜찮을까요?","recent":[{"date":"2026-06-21","appetite":"적게 먹음","activity":"조용함"},{"date":"2026-06-20","appetite":"보통","activity":"보통"}]}' | jq .
```

**다음 이슈**: FEAT-06 (발자국 읽기 — AI 요약 연동)

---

### FEAT-06 — AI 발자국 읽기 연동 클라이언트 (이슈 #6)

**추가 문서 읽기 이유 기록**: `lib/types.ts`(AISummary 구조·Blob 필드 확인), `lib/repos.ts`(entryRepo.update 시그니처), `app/today/page.tsx`(FEAT-04 저장 흐름·savedEntryId 자리), `app/api/ai/summary/route.ts`(응답 JSON 구조 확인), `lib/guardrails.ts`(PetLike/EntryLike 타입 확인) — 호출 지시에 명시된 범위 최소 확독.

**구현 완료 파일 목록**:
- `lib/aiClient.ts` — 신규 생성
  - `SummaryResponse` 인터페이스 export: `{ condition, behavior, observation, memory, vet_note: string | null }`
  - `requestSummary(pet, entry)`: POST /api/ai/summary 클라이언트 fetch 래퍼
  - Blob 제외 매핑: pet → `{ name, species, breed, age, gender, personality, likes, dislikes, health_notes }` (profile_image Blob 미포함)
  - entry → `{ date, diary_text, appetite, activity, sleep, toilet, unusual_behavior, mood_tags }` (photos Blob[] 미포함, 보정 지시 4)
  - 서버 오류(4xx/5xx) + 네트워크 오류 throw → 호출자가 try/catch 처리
- `components/AISummaryPanel.tsx` — 신규 생성
  - `AISummaryPanel({ summary: AISummary; loading?: boolean })` export
  - 4개 카드: "오늘의 컨디션" / "행동 이야기" / "내일의 관찰 포인트" / "오늘의 한 줄 추억"
  - 추억 문장은 `<blockquote>` 인용체 + 왼쪽 primary 라인 강조
  - `vet_note` 있을 때만 "살펴봐요 🐾" 부드러운 안내 카드 추가 렌더 (경고 톤 금지, AC-10)
  - 필드값 빈 문자열 → "내용을 준비하지 못했어요" 폴백 (앱 크래시 금지)
- `app/today/page.tsx` — FEAT-04 산출물 확장
  - `savedEntryId: string | undefined` → `savedEntry: DiaryEntry | undefined` 교체 (entry 객체 보관)
  - 추가 state: `aiLoading`, `aiError`, `aiSummary`
  - `triggerAI(entry)` async 함수: setAiLoading(true) → requestSummary → entryRepo.update({...entry, ai_summary}) → setAiSummary
  - 실패 시: setAiError + savedEntry는 그대로 (일기 본문 보존, 보정 지시 5)
  - 저장 완료 화면: AI 로딩 → Spinner + 안내 문구 / AI 에러 → 에러 카드 + "다시 읽기" 버튼 / AI 성공 → AISummaryPanel
  - handleSubmit: `entryRepo.save` 성공 후 `setSavedEntry(entry)` → `void triggerAI(entry)` (fire-and-forget, 보정 지시 5)

**저장→AI→병합 흐름 요약**:
```
handleSubmit:
  1. entryRepo.save()         ← 일기 저장 (ai_summary 없이)
  2. setSavedEntry(entry)     ← 저장 완료 화면 전환 (이미 데이터 보존)
  3. void triggerAI(entry)    ← AI 호출 fire-and-forget

triggerAI:
  1. setAiLoading(true)
  2. requestSummary(pet, entry)  ← Blob 제외 payload 전송
  3. entryRepo.update({ ...entry, ai_summary })  ← ai_summary 병합
  4. setAiSummary(aiSummaryData)
  실패 시: setAiError → entry 본문은 IndexedDB에 그대로 (보존)
```

**Blob 제외 매핑 방식**:
- `petPayload`: name/species/breed/age/gender/personality/likes/dislikes/health_notes — `profile_image` 필드 제외
- `entryPayload`: date/diary_text/appetite/activity/sleep/toilet/unusual_behavior/mood_tags — `photos`, `ai_summary`, `entry_id`, `created_at`, `pet_id` 필드 제외
- 구조 분해나 스프레드 대신 **명시적 필드 나열**로 Blob 포함 불가 보장 (보정 지시 4)

**빌드 결과**: `npm run build` 타입/빌드 에러 0 통과
```
Route (app)                              Size     First Load JS
┌ ○ /                                    175 B            96 kB
├ ○ /_not-found                          875 B            88 kB
├ ƒ /api/ai/ask                          0 B                0 B
├ ƒ /api/ai/summary                      0 B                0 B
├ ○ /profile/edit                        1.02 kB        94.1 kB
├ ○ /profile/new                         760 B          93.9 kB
└ ○ /today                               11 kB           110 kB  (+1 kB — AISummaryPanel 포함)
```

**빌드 우려 지점**: 없음. `AISummary.vet_note`는 `string | undefined`이고 `SummaryResponse.vet_note`는 `string | null`이므로 `response.vet_note != null ? { vet_note: ... } : {}` 스프레드 패턴으로 정확히 변환. 타입 에러 0.

**다음 이슈**: FEAT-07 (AI 질문하기)

---

### FEAT-07 — AI에게 질문하기 (이슈 #7)

**추가 문서 읽기 이유 기록**: `lib/aiClient.ts`(requestSummary 패턴 확인 — requestAsk 설계 참고), `lib/repos.ts`(conversationRepo.add/listByPet·entryRepo.recentByPet 시그니처), `lib/petContext.tsx`(usePet 확정 시그니처), `components/ui/*`(Button/Card/Tag/EmptyState/Spinner API), `components/AppHeader.tsx`(props), `app/today/page.tsx`(기존 페이지 패턴), `app/api/ai/ask/route.ts`(API 입출력 구조 확인), `lib/types.ts`(AIConversation 타입). 모두 호출 지시에 명시된 범위의 최소 확독.

**구현 완료 파일 목록**:
- `lib/aiClient.ts` — FEAT-06 산출물 확장
  - `requestAsk(pet, question, recent)` 함수 추가
  - pet 직렬화: profile_image(Blob) 제외, 텍스트/상태 필드만 전송 (보정 지시 4)
  - recent 직렬화: photos(Blob[]) 제외, `{ date, appetite, activity, sleep, toilet, unusual_behavior, mood_tags, condition: ai_summary?.condition }` 명시적 필드 매핑
  - 서버 오류(4xx/5xx) + 네트워크 오류 → Error throw (호출자 try/catch 처리)
- `app/ask/page.tsx` — 신규 생성
  - `usePet()` → loading/no-pet/메인 화면 3단 분기
  - activePet 없으면 EmptyState + `/profile/new` 유도
  - 예시 질문 칩 4개 (클릭 시 textarea 자동 채움)
  - 히어로 문구: "우리 아이에 대해 무엇이든 물어보세요 🐾"
  - textarea + "질문하기" 버튼 (빈 입력 차단, 전송 중 disabled)
  - `handleSubmit`: `entryRepo.recentByPet(pet_id, 5)` → `requestAsk` → `conversationRepo.add` → 목록 갱신 → 입력 초기화
  - AI 호출 중 Spinner + 안내 문구 표시
  - 실패 시: 에러 카드 + "다시 시도하기" 버튼, 질문 텍스트 유지
  - 과거 대화 목록: timestamp desc, "질문 카드 + 답변 카드" 쌍으로 표시
  - `useCallback(refreshConversations, [pet])` → `useEffect` 패턴으로 초기 로딩 + 제출 후 갱신 공용

**AI 컨텍스트 구성 (AC-11)**:
```
requestAsk payload = {
  pet: { name, species, breed, age, gender, personality, likes, dislikes, health_notes },
                       (profile_image Blob 제외)
  question: string,
  recent: [최근 5개] {
    date, appetite, activity, sleep, toilet, unusual_behavior, mood_tags, condition
                                                              (photos Blob[] 제외)
  }
}
```

**대화 저장·재조회 흐름 (AC-12)**:
```
handleSubmit 성공:
  1. requestAsk 응답 → conversationRepo.add({ pet_id, user_question, ai_response })
  2. refreshConversations() → conversations 상태 갱신
  3. 새 Q&A가 리스트 최상단에 렌더 (timestamp desc 정렬)
  4. 재진입 시: useEffect → refreshConversations → IndexedDB에서 재로딩
```

**빌드 결과**: `npm run build` 타입/빌드 에러 0 통과
```
Route (app)                              Size     First Load JS
┌ ○ /                                    175 B            96 kB
├ ○ /_not-found                          875 B            88 kB
├ ƒ /api/ai/ask                          0 B                0 B
├ ƒ /api/ai/summary                      0 B                0 B
├ ○ /ask                                 5.46 kB         101 kB
├ ○ /profile/edit                        1.02 kB        94.2 kB
├ ○ /profile/new                         760 B          93.9 kB
└ ○ /today                               11.2 kB         110 kB
+ First Load JS shared by all            87.1 kB
```

**빌드 우려 지점**: 없음.
- `useCallback` + `useEffect` 조합(refreshConversations)으로 react-hooks/exhaustive-deps 경고 없이 깔끔 처리.
- `formatTimestamp` catch 블록 파라미터 생략(TypeScript 4.0+ 지원) 정상 통과.
- `requestAsk`의 `recent` 명시적 필드 매핑으로 Blob 포함 불가 보장.

**다음 이슈**: FEAT-08 (기록 목록/열람 화면)

---

### FEAT-08 — 기록 목록 및 상세 보기 (이슈 #8)

**추가 문서 읽기 이유 기록**: `lib/repos.ts`(entryRepo.listByPet/get/remove/update 시그니처), `lib/types.ts`(DiaryEntry.ai_summary 구조), `components/AISummaryPanel.tsx`(props), `components/BlobImage.tsx`(props), `components/ui/{EmptyState,Tag,Button,Spinner}`, `lib/petContext.tsx`(usePet 시그니처), `app/today/page.tsx`(requestSummary 재시도 패턴) — FEAT-08 지시에 명시된 참고 파일 범위 최소 확독.

**구현 완료 파일 목록**:
- `lib/format.ts` — 신규 생성
  - `formatDateK(isoDate)`: 'YYYY-MM-DD' → '2026년 6월 22일 (월)'. `new Date(year, month-1, day)` 로컬 시간으로 요일 오프셋 방지.
  - `previewText(s?, n=60)`: 앞 n글자 초과 시 '…' 트리밍.
- `components/EntryCard.tsx` — 신규 생성
  - `EntryCard({ entry, onClick })`: 날짜(formatDateK) · mood_tags 최대 2개 Tag(selected) · previewText(ai_summary?.condition ?? diary_text) · photos[0] BlobImage 썸네일(있으면).
  - 버튼 요소로 구현 — onClick 클릭 가능, focus-visible 링 표시.
- `app/entries/page.tsx` — 신규 생성
  - `usePet()` + `entryRepo.listByPet(pet.pet_id)` 로드.
  - 정렬: listByPet 반환 순서(date desc · created_at desc) 그대로 사용 — 임의 재정렬 금지(보정 지시 7).
  - 0건: EmptyState("아직 발자국이 없어요") + "/today 오늘의 발자국 쓰기" 버튼.
  - activePet 없음: EmptyState + "/profile/new 프로필 등록하기" 버튼.
- `app/entries/[entryId]/page.tsx` — 신규 생성
  - `useParams()`로 entryId 추출 → `entryRepo.get(entryId)`.
  - 존재하지 않으면 EmptyState("기록을 찾을 수 없어요") + 목록 복귀 버튼.
  - 입력 전체 표시: 기분 태그 / 상태 4종 + 특이행동 / 자유 일기 / 사진 BlobImage 그리드.
  - ★ AI 요약: `entry.ai_summary`가 있으면 AISummaryPanel에 그대로 표시(재호출 금지, 보정 지시 6). 없으면 "AI 읽기가 아직 없어요" 안내 + "지금 읽기" 버튼(requestSummary → entryRepo.update, 선택 구현 완료).
  - 삭제: confirm 후 `entryRepo.remove` → router.push('/entries').
  - AppHeader 우측에 삭제 버튼 배치.

**AI 재호출 금지 처리 방식 (보정 지시 6)**:
```
entry.ai_summary 있음 → <AISummaryPanel summary={entry.ai_summary} /> (끝)
entry.ai_summary 없음 → "AI 읽기가 아직 없어요" 안내
                      + pet 있을 때만 "지금 읽기" 버튼
                        → handleRetryAI: requestSummary → entryRepo.update → setEntry(updated)
```
ai_summary가 있는 항목에서는 어떠한 조건에서도 재호출하지 않음.

**빌드 결과**: `npm run build` 타입/빌드 에러 0 통과
```
Route (app)                              Size     First Load JS
┌ ○ /                                    175 B            96 kB
├ ○ /_not-found                          875 B            88 kB
├ ƒ /api/ai/ask                          0 B                0 B
├ ƒ /api/ai/summary                      0 B                0 B
├ ○ /ask                                 5.46 kB         101 kB
├ ○ /entries                             2.71 kB         101 kB
├ ƒ /entries/[entryId]                   4.23 kB         102 kB
├ ○ /profile/edit                        1.02 kB          94 kB
├ ○ /profile/new                         761 B          93.7 kB
└ ○ /today                               13.6 kB         109 kB
+ First Load JS shared by all            87.1 kB
```

**빌드 우려 지점**: 없음.
- `useState<DiaryEntry | undefined | null>(undefined)` 타입에서, `if (!entry)` 통합 체크로 TypeScript 타입 좁히기 정상 작동 (null + undefined 동시 처리).
- `useParams()` Next.js 14 App Router에서 client 컴포넌트에 정상 적용.
- `entry.photos.map((blob, idx) =>` key로 인덱스 사용 — Blob 동일성 비교가 어려워 인덱스를 key로 사용하되 목록이 정적(변경 없음)이므로 문제 없음.

**다음 이슈**: FEAT-09 (홈 + 진입점 연결 — 마지막 이슈)

---

### FEAT-09 — 홈 진입 분기 + 네비 연결 + 마감 (이슈 #9)

**추가 문서 읽기 이유 기록**: 기존 FEAT-01~08 화면들(`page.tsx` 6개, `BottomNav.tsx`, `layout.tsx`, `globals.css`, `petContext.tsx`, `repos.ts`) — 홈 분기 로직·네비 연결 정합·CSS 변수 누락 확인 목적으로 최소 확독. 구현 지시에 명시된 참고 파일 범위.

**구현 완료 파일 목록**:
- `app/page.tsx` — FEAT-09 핵심: 홈 진입 분기 클라이언트 컴포넌트로 완전 재작성
  - `'use client'` 추가 (usePet/useRouter 사용)
  - `usePet()` → `{ pet, loading }` 구독
  - `useRef(false)` 로 중복 redirect 방지 (마운트 1회 보장)
  - `loading === false` → `router.replace(pet ? '/today' : '/profile/new')`
  - 5초 타임아웃(`SPLASH_TIMEOUT_MS=5000`) + "새로고침" 버튼 (무한 스플래시 방지 — FEAT-09 7절)
  - 대기 중 스플래시: 🐾 로고 + 타이틀 + Spinner
- `components/BottomNav.tsx` — 프로필 탭 활성 상태 수정
  - `NavItem` 인터페이스에 `activePrefix?: string` 추가 (`as const` 타입 완화)
  - 활성 판단 로직: `base = activePrefix ?? href` → `pathname === base || pathname.startsWith(base + '/')`
  - 프로필 탭: `{ href: '/profile/edit', activePrefix: '/profile' }` → `/profile/new` 방문 시도 활성화
  - `ul`에 `max-w-[480px] mx-auto` 추가 — 데스크톱 탭 중앙 정렬
- `app/globals.css` — `--card-bg` 누락 CSS 변수 추가
  - `--card-bg: #FFFFFF` — `entries/[entryId]/page.tsx` 섹션 배경에서 참조 중이나 미정의였음
- `app/layout.tsx` — 데스크톱 반응형 마감
  - `<main>` 에 `className="max-w-[480px] mx-auto w-full"` 추가
  - PetProvider 임시 골격 주석 제거 (FEAT-03에서 이미 실구현 완료)
  - BottomNav의 `ul` max-width와 layout의 `main` max-width가 480px로 통일됨

**홈 분기 로직 요약**:
```
/ 접속
  → PetProvider 마운트 → petRepo.getActive() 비동기 로딩
  → loading === true → 스플래시(🐾 + Spinner) 표시
  → loading === false →
      pet 있음: router.replace('/today')
      pet 없음: router.replace('/profile/new')
  → [예외] 5초 후에도 loading === true: "새로고침" 안내 표시
```

**★ 보정 지시 9 — Mock end-to-end 흐름 점검 결과**:
| # | 항목 | 코드 경로 | 결과 |
|---|------|-----------|------|
| 1 | 프로필 등록(`/profile/new`) → activePet 설정 → `/today` 이동 | `profile/new/page.tsx`: `petRepo.create` → `metaRepo.set('activePetId')` → `refresh()` → `router.push('/today')` | ✓ 연결됨 |
| 2 | 오늘의 발자국 작성·저장(`/today`) — 일기 본문 저장 성공 | `today/page.tsx`: STEP 1 `entryRepo.save({ pet_id, ...values })` 선저장 보장 (보정 지시 5) | ✓ 연결됨 |
| 3 | Mock AI 요약 4종 표시 + `entries.ai_summary`에 저장 | `today/page.tsx`: `triggerAI` → `requestSummary` → `entryRepo.update({ ...entry, ai_summary })`; `/api/ai/summary` 키 없으면 `buildMockSummary()` 반환 | ✓ 연결됨 |
| 4 | 기록 목록·상세에서 저장된 요약 재사용(재호출 없음) | `entries/[entryId]/page.tsx`: `entry.ai_summary` 있으면 `<AISummaryPanel>` 즉시 렌더, 재호출 없음 (보정 지시 6) | ✓ 연결됨 |
| 5 | AI 질문하기(`/ask`) Mock 응답 표시 + `conversations` 저장 | `ask/page.tsx`: `requestAsk` → `conversationRepo.add`; `/api/ai/ask` 키 없으면 `buildMockAsk()` 반환 | ✓ 연결됨 |
| 6 | 탭 종료 후 재진입 시 프로필·기록·대화 모두 유지(AC-15) | 모든 데이터 IndexedDB 저장(영구); 각 페이지 마운트 시 IndexedDB에서 재로드 | ✓ 연결됨 |

6항목 전부 코드 연결 확인됨. 키 없이 Mock 모드로 끊김 없는 흐름 성립.

**빌드 결과**: `npm run build` 타입/빌드 에러 0 통과
```
Route (app)                              Size     First Load JS
┌ ○ /                                    3.65 kB        90.8 kB   (175 B → 3.65 kB: 클라이언트 컴포넌트 전환)
├ ○ /_not-found                          875 B            88 kB
├ ƒ /api/ai/ask                          0 B                0 B
├ ƒ /api/ai/summary                      0 B                0 B
├ ○ /ask                                 4.26 kB         101 kB
├ ○ /entries                             1.5 kB          101 kB
├ ƒ /entries/[entryId]                   3.11 kB         103 kB
├ ○ /profile/edit                        1.02 kB        94.2 kB
├ ○ /profile/new                         761 B            94 kB
└ ○ /today                               10 kB           110 kB
+ First Load JS shared by all            87.1 kB
```

**빌드 우려 지점**: 없음.
- `useRef + useEffect` 조합으로 StrictMode 이중 마운트에서도 redirect 1회 보장.
- `max-w-[480px]` 추가 후 자식 페이지의 `min-h-screen` (= `min-height: 100vh`) 은 viewport 기준이므로 height 계산 영향 없음.
- `NavItem` 인터페이스 도입(`as const` 제거)으로 `activePrefix?: string` 선택 속성 타입 에러 없음.

---

## 의존성 버전 (package.json 기준)
| 패키지 | 버전 |
|--------|------|
| next | 14.2.29 |
| react | ^18.3.1 |
| react-dom | ^18.3.1 |
| typescript | ^5 |
| tailwindcss | ^3.4.14 |
| idb | ^8.0.0 |
| @anthropic-ai/sdk | ^0.27.0 |
| date-fns | ^3.6.0 |
